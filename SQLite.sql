-- SQLite

/*create table pos_register 
(
    id integer primary key autoincrement, 
    tag text, 
    posX decimal(5, 2), 
    posY decimal(5, 2), 
    posZ decimal(5, 2), 
    data DATETIME
);*/

select * 
from pos_register 
order by 1 desc;